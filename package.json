{
  "name": "webhands",
  "version": "0.1.0",
  "private": true,
  "description": "Computer-use agent for no-API tools. Drives real dashboards (TikTok Shop, supplier portals) via Cloudflare Browser Rendering, returns structured data, and gates any write behind an explicit confirm.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.98.0"
  }
}
